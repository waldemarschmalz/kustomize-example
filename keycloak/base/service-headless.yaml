apiVersion: v1
kind: Service
metadata:
  name: keycloak-headless
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: keycloak-x
    app.kubernetes.io/name: keycloakx
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http
  clusterIP: None
